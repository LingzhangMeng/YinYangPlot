% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/YinYangPlot.R
\name{yinyang}
\alias{yinyang}
\title{Create Yin-Yang Plot}
\usage{
yinyang(
  data,
  gene_id_col = "gene_id",
  gene_symbol_col = "gene_symbol",
  log2fc_col = "log2FoldChange",
  padj_col = "padj",
  significance_col = "significance",
  show_all = TRUE,
  top_n_up = 5,
  top_n_down = 5,
  gene_label_size = NULL,
  point_size_range = c(1, 7),
  background_alpha = 0.2,
  show_legend = TRUE,
  label_top_n = 20,
  title = "Yin-Yang Gene Regulation Map",
  subtitle = NULL,
  yin_color = "#90EE90",
  yang_color = "#FFB6C1",
  point_yin_color = "#006400",
  point_yang_color = "#8B0000",
  border_color = NULL,
  border_size = 1,
  min_radius = 0.3,
  max_radius = 0.85,
  radius_power = 1,
  plot_device_width = NULL,
  plot_device_height = NULL,
  seed = 42
)
}
\arguments{
\item{data}{A data frame containing differential expression results.
Must contain columns specified in `gene_id_col`, `gene_symbol_col`,
`log2fc_col`, `padj_col`, and `significance_col`.}

\item{gene_id_col}{Name of column containing unique gene identifiers.
Default: "gene_id"}

\item{gene_symbol_col}{Name of column containing gene symbols for labeling.
Default: "gene_symbol"}

\item{log2fc_col}{Name of column containing log2 fold change values.
Default: "log2FoldChange"}

\item{padj_col}{Name of column containing adjusted p-values.
Default: "padj"}

\item{significance_col}{Name of column indicating significance direction.
Should contain "Upregulated" or "Downregulated" for significant genes.
Default: "significance"}

\item{show_all}{Logical. If `TRUE`, show all significant genes.
If `FALSE`, show top `top_n_up` upregulated and `top_n_down` downregulated genes.
Default: `TRUE`}

\item{top_n_up}{Number of top upregulated genes to show when `show_all = FALSE`.
Default: 5}

\item{top_n_down}{Number of top downregulated genes to show when `show_all = FALSE`.
Default: 5}

\item{gene_label_size}{Font size for gene labels. If `NULL`, calculated automatically
based on available space and number of labels. Default: `NULL`}

\item{point_size_range}{Range for point sizes (minimum and maximum).
Default: `c(1, 7)`}

\item{background_alpha}{Alpha transparency for background semicircles (0-1).
Default: 0.2}

\item{show_legend}{Logical. Show legend on right side? Default: `TRUE`}

\item{label_top_n}{Number of top genes to label by significance.
Default: 20}

\item{title}{Plot title. Default: "Yin-Yang Gene Regulation Map"}

\item{subtitle}{Plot subtitle. If `NULL`, generated automatically.
Default: `NULL`}

\item{yin_color}{Background color for yin (downregulated) semicircle.
Default: "#90EE90" (light green)}

\item{yang_color}{Background color for yang (upregulated) semicircle.
Default: "#FFB6C1" (light pink)}

\item{point_yin_color}{Point color for downregulated genes.
Default: "#006400" (dark green)}

\item{point_yang_color}{Point color for upregulated genes.
Default: "#8B0000" (dark red)}

\item{border_color}{Color for plot border. If `NULL`, no border.
Default: `NULL`}

\item{border_size}{Size of plot border (if `border_color` not `NULL`).
Default: 1}

\item{min_radius}{Minimum radius for points (0-1). Default: 0.3}

\item{max_radius}{Maximum radius for points (0-1). Default: 0.85}

\item{radius_power}{Power transformation for radius scaling.
1 = linear, 2 = quadratic, 0.5 = square root. Default: 1}

\item{plot_device_width}{Width of plotting device in inches for auto-sizing.
If `NULL`, uses default. Default: `NULL`}

\item{plot_device_height}{Height of plotting device in inches for auto-sizing.
If `NULL`, uses default. Default: `NULL`}

\item{seed}{Random seed for reproducible gene placement. Default: 42}
}
\value{
A ggplot2 object representing the yin-yang plot.
}
\description{
Generates a circular yin-yang plot for visualizing differential gene expression.
In this plot, upregulated genes (Yang) appear on the right side, downregulated
genes (Yin) appear on the left side. Distance from the center represents
absolute log2 fold change, and point size represents statistical significance.
}
\details{
The yin-yang plot provides an intuitive visualization of differential gene expression:
\itemize{
  \item \strong{Yin (left side)}: Downregulated genes
  \item \strong{Yang (right side)}: Upregulated genes
  \item \strong{Distance from center}: Absolute log2 fold change (|log2FC|)
  \item \strong{Point size}: Statistical significance (-log10(adjusted p-value))
  \item \strong{Point color}: Regulation direction (green = down, red = up)
}
}
\examples{
\dontrun{
# Load example data
data(de_results)

# Basic yin-yang plot
p <- yinyang(data = de_results)
print(p)

# Customized plot
p_custom <- yinyang(
  data = de_results,
  show_all = FALSE,
  top_n_up = 10,
  top_n_down = 10,
  yin_color = "#E8F4F8",
  yang_color = "#FDEDEC",
  point_yin_color = "#2980B9",
  point_yang_color = "#E74C3C",
  title = "Differential Gene Expression",
  label_top_n = 15
)
print(p_custom)
}

}
